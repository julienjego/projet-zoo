import { SpeciesSociablePipe } from './species-sociable.pipe';

describe('SpeciesSociablePipe', () => {
  it('create an instance', () => {
    const pipe = new SpeciesSociablePipe();
    expect(pipe).toBeTruthy();
  });
});
