import { SpeciesSociableStringPipe } from './species-sociable-string.pipe';

describe('SpeciesSociableStringPipe', () => {
  it('create an instance', () => {
    const pipe = new SpeciesSociableStringPipe();
    expect(pipe).toBeTruthy();
  });
});
