import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'Zoo Thaï Koune',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
